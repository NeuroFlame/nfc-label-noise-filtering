# Pre-existing reference scripts, kept for documentation purposes only.
# Not part of the computation contract; not maintained to the boilerplate's
# lint/format standard. A nested config (rather than the migration-managed
# root pyproject.toml) so this survives future migrate_computation.py runs.
exclude = ["*"]
